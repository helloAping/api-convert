# 安全规则

- 网关密钥和上游渠道密钥是不同概念，不要混用。
- 网关密钥当前按业务要求同时保存明文和 SHA-256 哈希：明文仅用于管理端复制，哈希用于鉴权匹配。
- 上游渠道密钥、网关密钥明文在接口响应和业务日志中必须脱敏；复制功能除外。
- SQL 参数日志可能打印数据库参数值，生产环境开启 `SQL_LOG_LEVEL=DEBUG` 或 `SQL_PARAM_LOG_LEVEL=TRACE` 前必须确认不会泄露密钥明文。
- 不要提交本地凭证、上游 API Key、网关密钥或个人 agent 配置。
